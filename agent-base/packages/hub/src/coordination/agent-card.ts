import type { AgentCard,AgentSkill,SecurityRequirement } from "@a2a-js/sdk";import { A2A_VERSION,LOCAL_COORDINATION_EXTENSION } from "@agent-hub/contracts";
export function buildCoordinatorAgentCard(baseUrl:string,providers:string[]):AgentCard{const security:SecurityRequirement[]=[{schemes:{sessionBearer:{list:[]}}}];const skills:AgentSkill[]=[{id:"route-active-session",name:"Route work to an active local agent session",description:"Queues an attributed task for one explicitly selected delivery-capable local session.",tags:["local","session","delegation"],examples:["Ask session <id> to inspect the failing test"],inputModes:["text/plain","application/json"],outputModes:["text/plain","application/json"],securityRequirements:security}];if(providers.length)skills.push({id:"start-managed-worker",name:"Start a managed local agent worker",description:"Starts a worker through one explicitly named installed provider.",tags:["local","worker","delegation"],examples:["Start a worker in /workspace/repo to run tests"],inputModes:["text/plain","application/json"],outputModes:["text/plain","application/json"],securityRequirements:security});return{name:"Local Agent Coordinator",description:"Routes ephemeral tasks to explicitly selected local agent sessions or managed workers.",supportedInterfaces:[{url:baseUrl,protocolBinding:"HTTP+JSON",tenant:"",protocolVersion:A2A_VERSION}],provider:undefined,version:"0.1.0",capabilities:{streaming:false,pushNotifications:false,extendedAgentCard:false,extensions:[{uri:LOCAL_COORDINATION_EXTENSION,description:"Selects one local session or managed-worker target.",required:true,params:undefined}]},securitySchemes:{sessionBearer:{scheme:{$case:"httpAuthSecurityScheme",value:{description:"Ephemeral per-session task capability",scheme:"Bearer",bearerFormat:"opaque-256-bit"}}}},securityRequirements:security,defaultInputModes:["text/plain","application/json"],defaultOutputModes:["text/plain","application/json"],skills,signatures:[]}}
